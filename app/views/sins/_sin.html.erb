<div class="col-sm-3 single-sin-card" id=<%= sin_path(sin.id) %>>
  <div class="card">
    <div class="card-block">
      <h4 class="sin-text">
        <%= truncate(sin.title, :length => 60) %>
      </h4>
      <div class="sin-content">
        <div class="sined-by">Sined by <%= sin.user.name %></div>
        <h3><%= sin.title %></h3> 
        <p><%= sin.description %></p>
        <%= link_to "I'm interested", sin_path(sin.id), class: 'interested' %>
      </div>
    </div>
  </div><!-- card -->
</div><!-- col-sm-3 -->